[project]
name = "evajav"
version = "0.1.0"
description = "Sistema de evaluación docente anónima (FastAPI + Jinja2 + PostgreSQL)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.3",
    "jinja2>=3.1.6",
    "asyncpg>=0.31.0",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.32",
    "itsdangerous>=2.2.0",
]
